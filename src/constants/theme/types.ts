export type ColorSchemeType = {
  background: string,
  border: string,
  text: string,
  primary: string,
  spacing: { sm: number, md: number, lg: number },
  radius: { sm: number, md: number },
  shadow: string,
  tint: string,
  highlight: string,
  accent: string,
  warn: string,
  error: string,
};